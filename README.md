# bluetooth-rfcomm-device-controller
A lightweight RFCOMM-based device controller built with epoll, hierarchical state machine (HSM), custom binary protocol, CRC16-Modbus, and libgpiod GPIO control. Designed for embedded / IoT / Linux daemon environments.
